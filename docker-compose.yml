services:
  # This is your Web Server (Apache + PHP)
  php-apache:
    image: php:8.2-apache
    container_name: cleaning_web
    env_file:
      - .env
    environment:
      TZ: America/Los_Angeles
    volumes:
      - ./:/var/www/html
    ports:
      - "8080:80"
    command: sh -c "docker-php-ext-install pdo pdo_mysql && apache2-foreground"
    networks:
      - cleaning-network
    depends_on:
      - db

  # This is your Database (MySQL)
  db:
    image: mysql:8.0
    container_name: cleaning_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: cleaning_db
      TZ: America/Los_Angeles
    volumes:
      - db_data:/var/lib/mysql
    # We map port 3306 so you can connect to it from VS Code
    ports:
      - "3306:3306"
    networks:
      - cleaning-network

  # This is your Database Management Tool (phpMyAdmin)
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: cleaning_pma
    ports:
      - "8081:80"
    environment:
      PMA_HOST: db
      PMA_USER: root
      PMA_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    networks:
      - cleaning-network
    depends_on:
      - db

networks:
  cleaning-network:
    driver: bridge

volumes:
  db_data: